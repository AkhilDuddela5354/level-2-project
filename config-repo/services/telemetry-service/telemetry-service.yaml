# Telemetry Service - Base Configuration

server:
  port: 8082

spring:
  application:
    name: telemetry-service

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
